# Loop 201 candidates

1. **`format_turn_profile` honours TTY width** — wrap tools table on narrow terminals (carried; the meatier UX gap).
2. **`/tokens --json`** — round out the JSON-export pattern across the introspection commands.
3. **`/checkpoints export N <path> --gzip`** — compressed archives for long-term keeping.
4. **`format_history_diff` derives preview width from terminal**.
5. **Live vLLM smoke test** — environment-dependent; defer.

(2) finishes the JSON-export trilogy (lat/sysinfo/tokens). (1) is the next non-JSON candidate.
