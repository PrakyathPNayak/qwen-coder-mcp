# Next Loop Candidates
1. (P5) `_finish` and `_finish_no_file` shape unification.
2. (P5) Add a third README example showing an early-exit outcome.
3. (P6) `_log_aggregate_swallow_summary` records iteration count to sidecar file.
4. (P5) README update describing per-category delta breakdown in analytics.
5. (P5) Sister audit asserting every emit-site category appears in `OUTER_OUTCOME_CATEGORIES` (currently `_outer_outcome_category` falls back to raw token; a typo'd `applid:foo` would silently emit an undeclared category).
