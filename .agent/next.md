# Next Loop Candidates
1. (P3) Markdown rendering for assistant replies via rich.markdown.Markdown.
2. (P3) Tab completion for slash command names (Textual Input.suggester).
3. (P3) /diff with single argument compared against git HEAD instead of two files.
4. (P5) Status bar showing model name plus token meter persistently at the bottom of the screen.
5. (P5) Color theming via env var QWEN_TUI_THEME light or dark.
6. (P5) /history clear -- truncate the jsonl persistence file too.

