# Next Loop Candidates
1. (P3) Markdown rendering for assistant replies via rich.markdown.Markdown.
2. (P5) /history clear -- truncate the jsonl persistence file too.
3. (P5) /open <path> -- launch $EDITOR on a file from the chat.
4. (P5) Status bar widget showing model name plus running token meter.
5. (P5) /grep with explicit type filter (e.g. /grep TODO --py).
6. (P5) /pin <path> -- pin file to system prompt for the rest of the session.

