# Next Loop Candidates
1. (P5) `_finish` and `_finish_no_file` shape unification.
2. (P5) README update describing per-category delta breakdown.
3. (P6) `_log_aggregate_swallow_summary` records iteration count to sidecar file.
4. (P5) `--top-n` for category block too (when many categories accrue, show top by count).
5. (P5) `--since <iso>` flag to filter records by timestamp.
6. (P5) Verify all swallow-counter mutations occur via dedicated helpers.
