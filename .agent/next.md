# Loop 223 candidates

1. wait_ready.sh test - finishes the scripts/ dry-run coverage arc.
2. Real-model E2E with <tool_call> round trip (loop-217 followup).
3. /checkpoints export N <path> --gzip variant.
4. TUI prefix-buffering for unwrapped streaming </think> (loop-218 deferred).
5. /sysinfo --probe --json failure-shape audit.
6. run_loop.sh test.

Recommended: (1) - tightest scope, completes the scripts coverage arc.
