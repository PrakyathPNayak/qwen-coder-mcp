# Next Loop Candidates
1. (P3) Filesystem MCP tools `read_file`, `list_dir`, `write_file`, `apply_patch` (claude-code style).
2. (P3) Textual TUI client (`tui.py`) with chat view, slash-commands, tool palette.
3. (P5) Caching layer for `fetch_url` (in-memory LRU keyed by URL).
4. (P5) `web_search` `--region` and `--time-range` options (DDG params: kl, df).
5. (P5) `agent.timing_analyze` `--top-n` for category block too.
6. (P5) `_finish` and `_finish_no_file` shape unification.
