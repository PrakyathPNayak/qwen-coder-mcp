# Next Loop Candidates
1. (P3) Markdown rendering for assistant replies via rich.markdown.Markdown.
2. (P5) Status bar showing model name plus token meter persistently at the bottom of the screen.
3. (P5) Color theming via env var QWEN_TUI_THEME light or dark.
4. (P5) /history clear -- truncate the jsonl persistence file too.
5. (P5) /export to copy current chat as markdown to clipboard or file.
6. (P5) /sysinfo -- show backend health plus model plus loop counters.

