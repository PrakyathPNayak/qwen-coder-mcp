# Next loop seed

## Candidates ranked
1. **(P5) `_apply_diff` reject `index <sha>..<sha> 120000` / `160000`
   on the index line** — currently `_has_unsafe_mode` only scans the
   four explicit mode-header forms; git also encodes mode in
   `index abc..def 120000` for new symlinks.

2. **(P7) `_strip_fence` empty-language fence audit**.

3. **(P7) `qwen_client.system_user` kwargs passthrough contract test**.

4. **(P6) Clamp model diff size** before apply.

5. **(P8) `.agent/loop_log.md` rotation**.
