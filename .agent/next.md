# Next Loop Candidates
1. (P3) /model -- read or switch model id at runtime via client extra dict.
2. (P3) /sysprompt <text> -- replace the system prompt mid session.
3. (P5) Tab completion for slash command names (Textual Input.suggester).
4. (P5) Markdown rendering for assistant replies via rich.markdown.Markdown.
5. (P5) Persistent history across runs via .agent/history.jsonl read on startup.
6. (P5) /diff with single argument compared against git HEAD instead of two files.

