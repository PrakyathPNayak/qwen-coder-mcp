# Loop 163 candidates
1. Tool-calling layer (vLLM function-calling). Big risk; probably needs a feature flag.
2. Save-on-Ctrl-S so users don't lose history if Textual crashes between turns.
3. Session checkpoint 018-streaming-tui-extras.md -- still overdue.
4. /pin --tail <n> to inline only last n lines of a file.
5. /grep --count should also support @search inline result counts (cross-feature).
